import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	btn: {
		borderColor: "#ffffff",
		paddingVertical: 8,
		backgroundColor: "#ffffff",
		borderRadius: 8,
		alignItems: "center",
	},

	btnText: {
		color: "#744838",
		fontWeight: 600,
		fontSize: 12,
	},
});
